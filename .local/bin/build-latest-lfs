#!/bin/bash

for book in lfs blfs; do
  SOURCE_DIR=/var/lib/kiin/sources/${book}-git
  TARGET_DIR=/tmp/${book}-compiled
  rm -rf ${TARGET_DIR}
  cd ${SOURCE_DIR}
  git svn fetch
  git merge git-svn
  make BASEDIR=${TARGET_DIR} RENDERTMP=/tmp/${book}
done
