#!/bin/bash

cat /proc/bus/input/devices | grep -q Synaptics
if [ $? -eq 0 ]; then
    lsusb | grep -q "Microsoft Corp. Basic Optical Mouse"
    if [ $? -gt 0 ]; then
        synclient TouchpadOff=0
    else
        synclient TouchpadOff=1
    fi
fi
