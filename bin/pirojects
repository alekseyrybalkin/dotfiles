#!/bin/bash


for i in ~/projects/kiin{,-repo,-configs} ~/projects/{private,rybalkin.org} ~/; do
  cd $i
  if [ "${1}" == "pull" ]; then
    git pull
    sleep 5
  else
    echo "-------------------------------"
    echo "*** " $i
    git status
    echo
  fi
done
